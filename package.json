{
	"name": "Suresh Kumar Gadi's CV",
	"source": "src/index.html",
	"targets": {
		"default": {
			"distDir": "./docs"
		}
	},
	"scripts": {
		"clean:output": "rimraf docs",
		"start": "npm run clean:output && parcel",
		"build": "npm run clean:output && parcel build"
	},
	"devDependencies": {
		"parcel": "^2.7.0"
	},
	"dependencies": {
		"rimraf": "^3.0.2"
	}
}
